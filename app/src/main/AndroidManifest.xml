<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bimalka.smartbustracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".WebLoadingPage" />
        <activity android:name=".SingleMapDetailsPage" />
        <activity android:name=".CommonMapDetailsPage" />
        <activity android:name=".FavouriteTrackingsPage" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SingleMapDetailPage"
            android:label="@string/title_activity_single_map_detail_page" />
        <activity
            android:name=".CommonMapDetailPage"
            android:label="@string/title_activity_common_map_detail_page" />
        <activity android:name=".BusDetailsPage" />
        <activity android:name=".SaveFavouriteRoutes" />
        <activity
            android:name=".SingleMapDetailPage1"
            android:label="@string/title_activity_single_map_detail_page1" />
        <activity
            android:name=".SingleMapDetailsPage2"
            android:label="@string/title_activity_single_map_details_page2" />
        <activity
            android:name=".SingleMapDetailPage3"
            android:label="@string/title_activity_single_map_detail_page3" />
        <activity
            android:name=".SingleMapDetailPage4"
            android:label="@string/title_activity_single_map_detail_page4" />
        <activity
            android:name=".SingleMapDetailPage5"
            android:label="@string/title_activity_single_map_detail_page5" />
        <activity
            android:name=".SingleMapDetailPage6"
            android:label="@string/title_activity_single_map_detail_page6"></activity>
    </application>

</manifest>